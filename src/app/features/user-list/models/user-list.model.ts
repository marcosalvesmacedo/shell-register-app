export interface UserListResponse {
    id: number;
    name: string;
    email: string;
    userName: string;
}

export interface UserListAdaptedResponse {
    code: number;
    fullName: string;
    contact: string;
}